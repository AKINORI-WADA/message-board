<h1>メッセージ一覧</h1>
<%= form_for(@message) do |f| %>
    名前:<%= message.name %>
    内容:<%= message.body %>
    <br>
<% end %>

<%= form_for(@message) do |f| %>
    内容:
    <%= f.text_field :body %>
    <%= f.submit %>
<<% end %>